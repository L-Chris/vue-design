const setting = Object.freeze({
  label: 'section',
  tag: 'VdSection',
  originTag: 'section',
  category: 'layout',
  config: {
    Props: [
    ],
    Slots: [
      { name: 'default' }
    ]
  }
})

export default setting
