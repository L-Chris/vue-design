const setting = Object.freeze({
  label: 'i',
  tag: 'VdI',
  originTag: 'i',
  category: 'item',
  config: {
    Props: [
    ],
    Slots: [
      { name: 'default' }
    ]
  }
})

export default setting
