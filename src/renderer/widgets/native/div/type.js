const setting = Object.freeze({
  label: 'div',
  tag: 'VdDiv',
  originTag: 'div',
  category: 'layout',
  config: {
    Props: [
    ],
    Slots: [
      { name: 'default' }
    ]
  }
})

export default setting
